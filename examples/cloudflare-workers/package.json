{
  "name": "@fedify/cloudflare-workers-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "test": "true"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "^4.18.0"
  },
  "dependencies": {
    "@fedify/fedify": "workspace:^",
    "@fedify/vocab": "workspace:^"
  }
}
